1) ran_list_generator in rand generator file generates a list of n random numbers
direct api invocation using requests library


2)RGB Image generation:

packages required
1)PIL
2)numpy
3)requests

-array function generates random numbers integer array of 128*128
-numpy array is generated using these values
-then this numpy array is converted to image using PIL library


3)sound generation:
sound_gen file generates 3s sound file (samples per second = 44100)

4) RSA key pair generation
requirements
easy_install pycrypto
pycrypto is used to generate public and private keys
